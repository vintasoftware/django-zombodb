============
Installation
============

At the command line::

    $ easy_install django-zombodb

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-zombodb
    $ pip install django-zombodb
